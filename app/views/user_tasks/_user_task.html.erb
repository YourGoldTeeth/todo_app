<div class="container-fluid">
	<div class="well">
		<%= user_task.description %>
		<%= user_task.due %>
		<%= link_to "Edit", edit_user_task_path(user_task), remote: true %>
		<%= link_to "Destroy", user_task, remote: true, method: :delete, data: {confirm: "Are you sure you want to delete #{user_task.description}?"} %>
	</div>		
</div>